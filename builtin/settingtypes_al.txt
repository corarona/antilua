[*Antilua] [client]

#    Show active cheat names on the HUD.
cheat_hud (Cheat HUD) bool true

#    ---- Combat ----

#    Prevents knockback from attacks and explosions.
antiknockback (AntiKnockback) bool false

#    Makes the player float above attached entities (boats/minecarts).
float_above_parent (Float above parent) bool false

#    Auto-attack nearby entities (players/mobs/all modes).
killaura (Killaura) bool false

#    PatrolGuard bot — patrol area and engage targets using killaura strategy.
patrolguard (PatrolGuard) bool false

#    Build/place blocks below the player automatically.
scaffold (Scaffold) bool false

#    ---- Movement Cheats ----

#    Detached camera mode — fly through the world while the player stays.
freecam (Freecam) bool false

#    Look around with mouse without changing movement direction.
freelook (Freelook) bool false

#    Move continuously forward in the look direction.
#    Also togglable via the cheat menu.
continuous_forward (Continuous forward) bool false

#    Disable movement slowdown in cobwebs, etc.
no_slow (No Slowdown) bool false

#    Fly upward by holding the jump key mid-air.
jetpack (JetPack) bool false

#    Prevent slipping on slippery nodes like ice.
antislip (AntiSlip) bool false

#    Allows the player to jump in mid-air.
airjump (Air Jump) bool false

#    Climb up any walkable wall, not just climbable nodes.
spider (Spider) bool false

#    Walk on the surface of water/lava.
jesus (Jesus) bool false

#    Automatically jump over obstacles up to 0.5+ blocks high.
autojump (AutoJump) bool false

#    ---- Render / Visual ----

#    Hide non-xray nodes at the mesh level, revealing ores and caves.
xray (X-Ray) bool false

#    Comma-separated list of node names visible in X-Ray mode.
xray_nodes (X-Ray node list) string default:stone,mcl_core:stone

#    Force maximum light level on all nodes.
fullbright (Fullbright) bool false
fullbright.min_level (Fullbright min light level) int 15 0 15

#    Always render with daytime brightness.
no_night (Bright night) bool false

#    Ignore server-side HUD flags (health, breath, etc.).
hud_flags_bypass (HUD bypass) bool true

#    Disable the red-screen flash on damage.
no_hurt_cam (No hurt camera) bool false

#    Show the player's current position as an in-world HUD element.
coords (Coordinates) bool false

#    Allow pointing at/selecting liquid nodes.
point_all (Point All) bool false

#    Entity wireframe hitboxes visible through walls.
enable_entity_esp (Entity hitboxes) bool false

#    Lines from camera to each entity.
enable_entity_tracers (Entity tracers) bool false

#    Entity meshes rendered through walls (occluded entities only).
enable_entity_wallhack (Entity wallhack) bool false

#    Player wireframe hitboxes visible through walls.
enable_player_esp (Player hitboxes) bool false

#    Lines from camera to each player.
enable_player_tracers (Player tracers) bool false

#    Player meshes rendered through walls (occluded players only).
enable_player_wallhack (Player wallhack) bool false

#    Color for entity ESP/tracer lines (R,G,B).
entity_esp_color (Entity ESP color) string (255,255,255)

#    Color for player ESP/tracer lines (R,G,B).
player_esp_color (Player ESP color) string (255,255,255)

#    ---- Interact ----

#    Dig faster than normal.
fastdig (Fast digging) bool false

#    Place blocks faster than normal.
fastplace (Fast placement) bool false

#    Automatically dig the nearest diggable node.
autodig (Auto dig) bool false

#    Automatically place blocks where possible.
autoplace (Auto place) bool false

#    Break nodes in a single click.
instant_break (Instant break) bool false

#    Click repeatedly at high speed.
spamclick (Fast click / spamclick) bool false

#    Automatically hit nearby entities.
autohit (Auto hit) bool false

#    Max nodes processed per tick by scaffold/block tools.
nodes_per_tick (Nodes per tick) int 8 1 100

#    ---- Exploit ----

#    Entities move at full player speed (no lazy loading).
entity_speed (Entity speed) bool false

#    ---- Player ----

#    Prevent all environmental damage (fall, fire, lava, drowning).
prevent_natural_damage (No natural damage) bool true

#    Prevent forced rotation by server.
no_force_rotate (No force rotate) bool false

#    Extended interaction range.
#    Affects digging, placing, and entity interaction.
reach (Extended reach) bool true
reach.range (Reach range) float 6.6 1.0 100

#    Bypass privilege checks for fly/fast/noclip on the server.
priv_bypass (Privilege bypass) bool true

#    Automatically respawn on death.
autorespawn (Auto respawn) bool false

#    Prevent pointing at/selecting any node.
dont_point_nodes (Don't point nodes) bool false

#    Enable Lua remote control.
lua_control (Lua control) bool false

#    Experimental: separate camera from player.
detached_camera (Detached camera) bool false

#    Use toggle mode (press TAB to open/close) instead of hold-to-show.
cheat_menu_toggle_mode (Cheat menu toggle mode) bool false

#    Comma-separated list of favorited cheat settings for the favorites panel.
cheat_menu_favorites (Cheat menu favorites) string

[*Antilua Cheat Menu Theme] [client]

#    Font type for the cheat menu text.
cheat_menu_font (Cheat menu font) string FM_Standard

#    Background color of the cheat menu overlay (hex #RRGGBB).
theme_bg (Overlay background) string #121212

#    Background opacity of the overlay (0-255).
theme_bg_alpha (Overlay background opacity) int 191 0 255

#    Background color of cheat menu panels (hex #RRGGBB).
theme_panel_bg (Panel background) string #000000

#    Panel background opacity (0-255).
theme_panel_bg_alpha (Panel background opacity) int 204 0 255

#    Background color of the panel title bar (hex #RRGGBB).
theme_title_bg (Title bar background) string #1a4a1a

#    Title bar opacity (0-255).
theme_title_bg_alpha (Title bar opacity) int 204 0 255

#    Panel border highlight color (hex #RRGGBB).
theme_border (Border color) string #0a0a0a

#    Panel border opacity (0-255).
theme_border_alpha (Border opacity) int 204 0 255

#    Background color of inactive cheat items (hex #RRGGBB).
theme_item_bg (Item background) string #000000

#    Item background opacity (0-255).
theme_item_bg_alpha (Item background opacity) int 204 0 255

#    Background color of active/enabled cheat items (hex #RRGGBB).
theme_active_bg (Active item background) string #2d2d37

#    Active item background opacity (0-255).
theme_active_bg_alpha (Active item background opacity) int 204 0 255

#    Normal font color (hex #RRGGBB).
theme_text (Normal text color) string #00cc00

#    Font opacity (0-255).
theme_text_alpha (Normal text opacity) int 255 0 255

#    Selected/highlighted font color (hex #RRGGBB).
theme_selected_text (Selected text color) string #ffffff

#    Tooltip background color (hex #RRGGBB).
theme_tooltip_bg (Tooltip background) string #000000

#    Height of the category header row in pixels.
cheat_menu_head_height (Cheat menu head height) int 33 10 200

#    Height of each cheat entry in pixels.
cheat_menu_entry_height (Cheat menu entry height) int 35 10 200

#    Width of the cheat menu in pixels.
cheat_menu_entry_width (Cheat menu entry width) int 200 50 500

[*Antilua DTE] [client]

#    Width of the DTE formspec window.
dte_width (DTE window width) float 15 8 40

#    Height of the DTE formspec window.
dte_height (DTE window height) float 10 6 30
